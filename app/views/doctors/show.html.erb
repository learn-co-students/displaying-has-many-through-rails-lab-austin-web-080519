<%= @doctor.name %>
<%= @doctor.department %>
<% @doctor.appointments.each do |a| %>
 <p>appointment at <%= a.appointment_datetime.strftime("%B %d, %Y at %R") %> with <%= link_to a.patient.name, patient_path(a.patient) %></p>
<% end %>
